apiVersion: openchoreo.dev/v1alpha1
metadata:
  name: github-sync-webapp

endpoints:
  - name: http
    type: HTTP
    port: 9090
    visibility:
      - project
      - external

dependencies:
  endpoints:
    - component: sync-github-issues-github-sync-api
      name: http
      visibility: project
      envBindings:
        address: GITHUB_SYNC_API_URL
  resources:
    - ref: sync-github-issue-24469684
      envBindings:
        client_id: USER_AUTH_CLIENT_ID
        issuer: USER_AUTH_ISSUER
        jwks_url: USER_AUTH_JWKS_URL
        resource: USER_AUTH_RESOURCE
        scopes: USER_AUTH_SCOPES
